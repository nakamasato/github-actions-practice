name: failure

on: pull_request

jobs:
  test1: # succeed
    runs-on: ubuntu-latest
    steps:
      - id: fail_step
        continue-on-error: true
        run: exit 1

      - run: echo succeeded or failed
      
      - if: failure() && steps.fail_step.outcome == 'failure'
